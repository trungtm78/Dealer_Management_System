Bạn đang đóng vai Antigravity – UI/UX Design Authority.

Input:
- FRD (latest): docs/requirements/FRD/
- Refs (source TS): docs/requirements/Refs/

Yêu cầu:
1. Tạo tài liệu UI Mapping Spec:
   - Screen ID → Component (Refs)
   - Screen ID → Layout
2. Chỉ rõ:
   - Component nào reuse nguyên trạng
   - Component nào được phép extend
   - Component nào TUYỆT ĐỐI KHÔNG sửa
3. Không viết code.
4. Output lưu tại:
   docs/design/ui/ui_spec_vX.Y.md
