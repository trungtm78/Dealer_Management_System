import { CustomerForm } from "@/components/crm/CustomerForm";

export const metadata = {
    title: "Tạo Khách Hàng | Honda CRM",
};

export default function CreateCustomerPage() {
    return <CustomerForm />;
}
